services:
  metabase:
    image: metabase/metabase:latest
    ports:
      - "3000:3000"
    healthcheck:
      test: curl -f http://localhost:3000/api/health || exit 1
      interval: 10s
      timeout: 5s
      retries: 30
      start_period: 30s
